*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
.kotlin
.vscode

# Eclipse / JDT Language Server (jdtls) generated files
.classpath
.project
.settings/

local.properties
*.log
org.difft.*.txt
# Claude Code local settings (user-specific)
.claude/settings.local.json
# Per-workspace active-task pointer (runtime state; kept out of the shared tmp/ so
# workspaces that symlink a common tmp/ don't clobber each other's current task)
.claude/current-task
CLAUDE.local.md
tmp/
