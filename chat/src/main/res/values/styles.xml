<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="TransparentStatusBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/bg1.night</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/bg1.night</item>
    </style>


    <style name="EventEditorLabelStyle">
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:lineSpacingExtra">6sp</item>
        <item name="android:textColor">@color/t.primary</item>
        <item name="android:textSize">14sp</item>
    </style>



    <!-- Text Preview Activity Theme with fade animation -->
    <style name="Theme.TextPreview" parent="Theme.Chative">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@style/TextPreviewAnimation</item>
        <!-- Selection handle color -->
        <item name="colorControlActivated">@color/primary</item>
    </style>

    <style name="TextPreviewAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/text_preview_fade_in</item>
        <item name="android:activityOpenExitAnimation">@anim/text_preview_fade_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/text_preview_fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/text_preview_fade_out</item>
    </style>

</resources>
